# Valores para desenvolvimento local (frontend via `npm run dev` na porta 5173,
# falando direto com o backend rodando sozinho na porta 3333 — sem Nginx no meio).
VITE_API_URL=http://localhost:3333/api
VITE_SOCKET_URL=http://localhost:3333
